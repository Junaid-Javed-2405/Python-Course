s={}
# this will create an empty set. We cannot create a set using s=set() because it will create an empty dictionary instead of a set. To create an empty set, we need to use s=set().

print(type(s)) # this will print <class 'set'> which means s is a set