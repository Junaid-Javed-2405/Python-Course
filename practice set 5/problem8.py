s={1,2,3,"harry","ron",1.5,{1,2}}

# this will give an error because set cannot contain a set as an element. We can only add immutable elements to a set, such as integers, strings, and tuples. We cannot add mutable elements, such as lists and sets, to a set.